{
  "skill_name": "zoowork-managed-agents",
  "notes": "Assertions run against the code an assistant produces, not against a live deployment. `present` passes when any pattern matches, `absent` passes when none do, and `order` passes when each pattern first appears after the previous one. Patterns are JavaScript regular expressions, case-sensitive. See README.md.",
  "evals": [
    {
      "id": 1,
      "name": "first-integration-from-zero",
      "prompt": "I have a ZooWork API key (the zct_ kind). Write me a small Node script I can run with tsx that creates an agent, asks it 'what is the capital of France', prints the answer as it streams, and then deletes the agent again so I don't leave junk behind.",
      "expected_output": "A TypeScript file importing @zoowork-ai/sdk that creates an agent, starts it, waits on desired_state (ideally via waitUntilRunning), opens a session with an initial user.message, streams until run.finished, then stops and deletes.",
      "files": [],
      "assertions": [
        {
          "name": "imports the real package name",
          "kind": "present",
          "patterns": [
            "@zoowork-ai/sdk"
          ]
        },
        {
          "name": "does not invent a package name or reach for the deprecated one",
          "kind": "absent",
          "patterns": [
            "@zoowork/sdk",
            "@zoowork/agents-sdk",
            "@zoowork-agents/sdk",
            "['\"]zoowork['\"]",
            "@zooclaw"
          ]
        },
        {
          "name": "starts the agent after creating it",
          "kind": "order",
          "patterns": [
            "createAgent",
            "startAgent"
          ]
        },
        {
          "name": "waits on desired_state, or lets the SDK do it",
          "kind": "present",
          "patterns": [
            "waitUntilRunning",
            "desired_state"
          ]
        },
        {
          "name": "passes agentId to createSession",
          "kind": "present",
          "patterns": [
            "createSession\\(\\s*agentId",
            "createSession\\(\\s*[a-zA-Z_$][\\w$]*\\s*,"
          ]
        },
        {
          "name": "ends the turn on run.finished instead of waiting for the stream to close",
          "kind": "present",
          "patterns": [
            "isRunFinished",
            "run\\.finished"
          ]
        },
        {
          "name": "does not build the reply out of delta frames",
          "kind": "absent",
          "patterns": [
            "\\.delta\\??\\.",
            "\\bdelta\\s*\\+="
          ]
        },
        {
          "name": "reads assistant text through the SDK rather than digging into payload",
          "kind": "present",
          "patterns": [
            "assistantText\\(",
            "agent\\.assistant"
          ]
        },
        {
          "name": "stops before deleting",
          "kind": "order",
          "patterns": [
            "stopAgent",
            "deleteAgent"
          ]
        }
      ]
    },
    {
      "id": 2,
      "name": "host-a-locally-built-agent",
      "prompt": "I built a deck-editing assistant locally with Claude Code. It's a system prompt in AGENTS.md plus two skill folders (slide-layout/ and chart-builder/, each with a SKILL.md). My front end is fine, I host that myself - what I don't have is anywhere for the agent and its skills to actually run. I have a ZooWork key. Set this up for me.",
      "expected_output": "A provisioning script that creates an agent carrying the system prompt as persona.docs, zips each skill directory with the top-level directory name matching the SKILL.md frontmatter name, uploads each with a scope, attaches them, and verifies the attachment with listAgentSkills.",
      "files": [],
      "assertions": [
        {
          "name": "puts the system prompt into persona.docs",
          "kind": "present",
          "patterns": [
            "persona",
            "docs"
          ]
        },
        {
          "name": "uploads the skills through the SDK",
          "kind": "present",
          "patterns": [
            "uploadSkill"
          ]
        },
        {
          "name": "passes a scope on upload",
          "kind": "present",
          "patterns": [
            "scope:\\s*['\"](org|personal)['\"]"
          ]
        },
        {
          "name": "surfaces the zip top-level directory rule",
          "kind": "present",
          "patterns": [
            "top-level",
            "top level",
            "directory name",
            "frontmatter"
          ]
        },
        {
          "name": "attaches after uploading",
          "kind": "order",
          "patterns": [
            "uploadSkill",
            "putAgentSkill"
          ]
        },
        {
          "name": "verifies the attachment rather than trusting the response",
          "kind": "present",
          "patterns": [
            "listAgentSkills"
          ]
        },
        {
          "name": "uses an idempotency key so a re-run does not create a second agent",
          "kind": "present",
          "patterns": [
            "dempotency",
            "idempotencyKey"
          ]
        },
        {
          "name": "keeps the key server-side",
          "kind": "absent",
          "patterns": [
            "VITE_ZOOWORK",
            "NEXT_PUBLIC_ZOOWORK",
            "REACT_APP_ZOOWORK"
          ]
        }
      ]
    },
    {
      "id": 3,
      "name": "export-a-long-transcript",
      "prompt": "Our support agent has sessions that run to several hundred messages. Write a function that takes an agent id and a session id and exports the complete transcript of that session to a JSON file. It has to be complete - we're using it for compliance review.",
      "expected_output": "Use listAllEvents, or loop listEventsPage with hasMore and nextCursor until exhausted. Include both user and assistant messages. getSession({history:true,limit}) is only a bounded recent window, not a complete export.",
      "files": [],
      "assertions": [
        {
          "name": "reads the whole session rather than one page",
          "kind": "present",
          "patterns": [
            "\\blistAllEvents\\s*\\(",
            "(?=[\\s\\S]*\\blistEventsPage\\s*\\()(?=[\\s\\S]*\\b(?:while|for)\\s*\\()(?=[\\s\\S]*\\bhasMore\\b)(?=[\\s\\S]*\\bcursor\\s*[:=][\\s\\S]*\\bnextCursor\\b)"
          ]
        },
        {
          "name": "does not treat a single listEvents call as complete",
          "kind": "absent",
          "patterns": [
            "listEvents\\([^)]*\\)\\s*;?\\s*\\n[^\\n]*writeFile"
          ]
        }
      ]
    },
    {
      "id": 4,
      "name": "capability-boundary-custom-tool",
      "prompt": "Mid-conversation I want the agent to call our internal pricing API to look up a quote for the customer, get the number back, and keep going in the same turn using that number. Wire that up against ZooWork.",
      "expected_output": "Declare an application-executed custom tool under resource.custom_tools, consume agent.custom_tool_use, execute the pricing call in the application, resolve it in the same run, and recover pending calls after restarts. Mark the contract source-reviewed rather than live-verified.",
      "files": [],
      "assertions": [
        {
          "name": "declares the application-executed tool",
          "kind": "present",
          "patterns": [
            "custom_tools[\\s\\S]{0,400}lookup_price",
            "custom_tools[\\s\\S]{0,400}pricing"
          ]
        },
        {
          "name": "reads the custom tool request event",
          "kind": "present",
          "patterns": [
            "customToolUse\\s*\\(",
            "agent\\.custom_tool_use"
          ]
        },
        {
          "name": "returns the result to the waiting run",
          "kind": "present",
          "patterns": [
            "resolveCustomToolCall\\s*\\(",
            "user\\.custom_tool_result"
          ]
        },
        {
          "name": "recovers pending work after restart",
          "kind": "present",
          "patterns": [
            "listCustomToolCalls[\\s\\S]{0,120}pending",
            "pending[\\s\\S]{0,120}listCustomToolCalls"
          ]
        },
        {
          "name": "does not overclaim live verification",
          "kind": "present",
          "patterns": [
            "source-reviewed",
            "not (?:deployment-|live-)?verified",
            "源码(?:已)?(?:审查|审阅|核对)[\\s\\S]{0,160}(?:尚未|没有|未完成).{0,40}(?:线上部署|真实部署|端到端)"
          ]
        }
      ]
    },
    {
      "id": 5,
      "name": "reconnect-a-dropped-stream",
      "prompt": "Our ZooWork event stream keeps dying partway through long tasks and we lose the rest of the answer. We're using streamEvents in a loop. Make it survive a dropped connection.",
      "expected_output": "A bounded reconnect loop uses the opaque ev.cursor as a cursor query option, checkpoints after processing, respects caller cancellation and handles structured or non-JSON open errors. No new after/seq-based reconnect or Last-Event-ID-only public-gateway replay.",
      "files": [],
      "assertions": [
        {
          "name": "resumes using a cursor option",
          "kind": "present",
          "patterns": [
            "streamEvents\\([\\s\\S]*?\\{[\\s\\S]*?\\bcursor\\b"
          ]
        },
        {
          "name": "reads the opaque cursor from an event",
          "kind": "present",
          "patterns": [
            "\\b(?:ev|event|e)\\.cursor\\b"
          ]
        },
        {
          "name": "does not select the legacy after lane",
          "kind": "absent",
          "patterns": [
            "\\bafter\\s*:\\s*(?:0\\b|last\\w*|[a-zA-Z_$][\\w$]*Seq\\b|[a-zA-Z_$][\\w$]*\\.seq\\b)"
          ]
        },
        {
          "name": "respects an abort instead of reconnecting forever",
          "kind": "present",
          "patterns": [
            "signal",
            "abort",
            "Abort"
          ]
        }
      ]
    },
    {
      "id": 6,
      "name": "no-key-yet-onboarding",
      "prompt": "I want to try ZooWork managed agents from my Node backend. I don't have an API key or anything set up yet - where do I even start?",
      "expected_output": "Guides the user to create a key at https://zoowork.ai/claw-settings?tab=account-api-keys (Settings -> API Keys), warns the secret is shown exactly once, has them save it into ZOOWORK_API_KEY themselves without pasting it into the chat, and verifies with listModels() before writing integration code.",
      "files": [],
      "assertions": [
        {
          "name": "gives the real console URL instead of guessing a signup page",
          "kind": "present",
          "patterns": [
            "zoowork\\.ai/claw-settings\\?tab=account-api-keys"
          ]
        },
        {
          "name": "warns the secret is shown exactly once",
          "kind": "present",
          "patterns": [
            "[Ss]hown (exactly )?once",
            "only (be )?shown once",
            "[Cc]opy (it|the (key|secret)) (immediately|right away|now)"
          ]
        },
        {
          "name": "has the user save the key themselves rather than paste it into chat",
          "kind": "present",
          "patterns": [
            "not (to )?paste.{0,40}(chat|here|conversation)",
            "([Dd]on'?t|[Dd]o not|[Nn]ever) (paste|share).{0,60}(chat|here|with me|conversation)",
            "without (sharing|pasting) (it|the key)"
          ]
        },
        {
          "name": "verifies the key with the cheap read-only call before real code",
          "kind": "present",
          "patterns": [
            "listModels"
          ]
        }
      ]
    },
    {
      "id": 7,
      "name": "attribute-two-users-without-trusting-input",
      "prompt": "Two signed-in customers share our ZooWork agent. Show how our Node backend should attribute each user.message, including initial_events. The browser can submit arbitrary JSON. Is this enough to isolate their files and authorize session reads?",
      "expected_output": "Backend-selected stable actor.ref, never a token or arbitrary browser actor. Explain attribution is not authorization or file/session isolation; metadata alone is insufficient. Keep per-user session authorization and choose separate agents if agent-scope files must not be shared.",
      "files": [],
      "assertions": [
        {
          "name": "sets an actor reference on the message",
          "kind": "present",
          "patterns": [
            "actor\\s*:\\s*\\{\\s*ref\\s*:"
          ]
        },
        {
          "name": "does not send a credential as actor.token",
          "kind": "absent",
          "patterns": [
            "actor\\s*:\\s*\\{[^}]*\\btoken\\s*:"
          ]
        },
        {
          "name": "keeps authorization separate",
          "kind": "present",
          "patterns": [
            "[Aa]uthoriz",
            "[Aa]uthenticat"
          ]
        },
        {
          "name": "explains file isolation separately",
          "kind": "present",
          "patterns": [
            "[Ff]ile.{0,90}(isolat|shar)",
            "(isolat|shar).{0,90}[Ff]ile"
          ]
        }
      ]
    },
    {
      "id": 8,
      "name": "interval-and-skill-version-contracts",
      "prompt": "Write TypeScript snippets to create a ZooWork schedule every 60 seconds with a stable ID, inspect an optional linked session from a run, and upload a new version of our existing skill ZIP. Print the uploaded version and its state. Can I reuse latest_version from the root skill response?",
      "expected_output": "Schedule input uses everyMs:60000, not every. Guard optional run.session_id. uploadSkillVersion returns version/state, not root latest_version/status. Do not imply the snippet was live-tested.",
      "files": [],
      "assertions": [
        {
          "name": "uses interval milliseconds",
          "kind": "present",
          "patterns": [
            "everyMs\\s*:\\s*(60_?000|60\\s*\\*\\s*1000)"
          ]
        },
        {
          "name": "does not send the old every field",
          "kind": "absent",
          "patterns": [
            "\\bevery\\s*:"
          ]
        },
        {
          "name": "reads version and state",
          "kind": "present",
          "patterns": [
            "(?=[\\s\\S]*uploadSkillVersion\\s*\\()(?=[\\s\\S]*\\.version\\b)(?=[\\s\\S]*\\.state\\b)"
          ]
        },
        {
          "name": "checks optional session association",
          "kind": "present",
          "patterns": [
            "(?:if\\s*\\([^)]*session_id|session_id\\s*\\?|session_id\\s*&&)"
          ]
        }
      ]
    },
    {
      "id": 9,
      "name": "channel-allowlist-boundary",
      "prompt": "I passed allow_from with two sender IDs to addChannel. Can I rely on that as the only access control? Also, does binding Feishu make those conversations inaccessible to our organization API key?",
      "expected_output": "Public gateway ignores allow_from; it is not an ACL. dm_policy affects channel policy. Separate conversation context does not make IM sessions inaccessible to an org key. Backend end-user authorization remains required.",
      "files": [],
      "assertions": [
        {
          "name": "identifies ignored allow_from",
          "kind": "present",
          "patterns": [
            "allow_from[\\s\\S]{0,180}ignor",
            "ignor[\\s\\S]{0,180}allow_from"
          ]
        },
        {
          "name": "mentions effective dm_policy",
          "kind": "present",
          "patterns": [
            "dm_policy"
          ]
        },
        {
          "name": "retains backend authorization",
          "kind": "present",
          "patterns": [
            "[Bb]ackend[\\s\\S]{0,180}authoriz",
            "authoriz[\\s\\S]{0,180}[Bb]ackend"
          ]
        }
      ]
    },
    {
      "id": 10,
      "name": "invalid-write-versus-no-op-interrupt",
      "prompt": "postEvents sometimes rejects and sometimes returns accepted:false. Show a TypeScript handler that distinguishes an invalid event body from cancelling a session with no run in flight. Should I retry either one?",
      "expected_output": "Catch ZooworkError HTTP400 for invalid input, not transient retry. A valid interrupt with no run returns 202 accepted:false as a no-op. Do not promise atomic rollback for a batch.",
      "files": [],
      "assertions": [
        {
          "name": "handles HTTP400 as a thrown error",
          "kind": "present",
          "patterns": [
            "(?=[\\s\\S]*\\bcatch\\b)(?=[\\s\\S]*\\.status\\s*===?\\s*400)"
          ]
        },
        {
          "name": "handles a nonaccepted receipt separately",
          "kind": "present",
          "patterns": [
            "\\.accepted\\s*===?\\s*false",
            "accepted\\s*:\\s*false"
          ]
        },
        {
          "name": "does not advise retrying invalid input unchanged",
          "kind": "present",
          "patterns": [
            "[Dd]o not retry",
            "[Dd]on't retry",
            "[Ff]ix (the )?(body|input|event)",
            "[Nn]o.?op"
          ]
        }
      ]
    },
    {
      "id": 11,
      "name": "configure-mcp-tool-exposure",
      "prompt": "Our public unauthenticated HTTP MCP server has hundreds of tools. Show the ZooWork TypeScript declarations for keeping its tools lazy by default and for exposing them on the first model request. Is exposure:auto supported? If a lazy tool is loaded, will it still be available on the next turn in the same session?",
      "expected_output": "Uses McpServerDeclaration exposure deferred/direct, explains omission defaults to deferred and there is no auto value, and says a loaded deferred tool remains available on later turns in the same Session.",
      "files": [],
      "assertions": [
        {
          "name": "shows deferred exposure",
          "kind": "present",
          "patterns": [
            "exposure\\s*:\\s*['\"]deferred['\"]"
          ]
        },
        {
          "name": "shows direct exposure",
          "kind": "present",
          "patterns": [
            "exposure\\s*:\\s*['\"]direct['\"]"
          ]
        },
        {
          "name": "rejects an auto mode",
          "kind": "present",
          "patterns": [
            "[Nn]o.{0,20}['\"]?auto",
            "['\"]auto['\"].{0,40}(not supported|invalid|not a valid)"
          ]
        },
        {
          "name": "preserves a loaded deferred tool across turns in one session",
          "kind": "present",
          "patterns": [
            "(?:loaded|available)[\\s\\S]{0,120}(?:later|next|subsequent)[ -]turns?[\\s\\S]{0,120}(?:same|one)[ -]?[Ss]ession",
            "(?:same|one)[ -]?[Ss]ession[\\s\\S]{0,120}(?:later|next|subsequent)[ -]turns?[\\s\\S]{0,120}(?:loaded|available)"
          ]
        }
      ]
    },
    {
      "id": 12,
      "name": "mcp-runtime-context-and-permissions",
      "prompt": "Configure our public ZooWork MCP server so each tool call receives agent/session context in MCP metadata, every tool asks for approval by default, but the native tool named lookup_customer is always allowed. Explain whether this authenticates the caller and what an allow-always decision at server scope grants.",
      "expected_output": "Use context.meta, server permission always_ask, and an exact tools.lookup_customer always_allow override. State that runtime context is not authentication and server-scope allow-always covers every server tool for that Session.",
      "files": [],
      "assertions": [
        {
          "name": "opts into MCP metadata context",
          "kind": "present",
          "patterns": [
            "context\\s*:\\s*\\{[\\s\\S]{0,80}meta\\s*:\\s*true"
          ]
        },
        {
          "name": "sets a server approval default",
          "kind": "present",
          "patterns": [
            "permission\\s*:\\s*['\"]always_ask['\"]"
          ]
        },
        {
          "name": "uses an exact native tool override",
          "kind": "present",
          "patterns": [
            "lookup_customer[\\s\\S]{0,100}permission\\s*:\\s*['\"]always_allow['\"]"
          ]
        },
        {
          "name": "does not treat context as authentication",
          "kind": "present",
          "patterns": [
            "context[\\s\\S]{0,100}(?:not|isn't|is not)[\\s\\S]{0,60}auth",
            "(?:not|isn't|is not)[\\s\\S]{0,60}auth[\\s\\S]{0,100}context"
          ]
        },
        {
          "name": "explains server wildcard grant scope",
          "kind": "present",
          "patterns": [
            "allow-always[\\s\\S]{0,180}(?:all|every)[\\s\\S]{0,80}(?:server|Session)",
            "server[ -](?:scope|wide|wildcard)[\\s\\S]{0,180}(?:all|every)[\\s\\S]{0,60}tool"
          ]
        }
      ]
    },
    {
      "id": 13,
      "name": "tool-policy-prefix-patterns",
      "prompt": "I want a ZooWork tool policy that applies to every tool from MCP server pricing, but alsoAllow should grant only one exact tool. Which wildcard forms actually work? Please show the relevant object and call out mcp__*__quote and *search.",
      "expected_output": "Use a single trailing prefix pattern such as mcp__pricing__*, keep alsoAllow exact, and say embedded or leading wildcard forms match nothing. Mention global * and exact names as the other supported forms.",
      "files": [],
      "assertions": [
        {
          "name": "uses the supported trailing-prefix pattern",
          "kind": "present",
          "patterns": [
            "mcp__pricing__\\*"
          ]
        },
        {
          "name": "keeps alsoAllow exact",
          "kind": "present",
          "patterns": [
            "alsoAllow[\\s\\S]{0,180}exact",
            "exact[\\s\\S]{0,180}alsoAllow"
          ]
        },
        {
          "name": "rejects unsupported wildcard placements",
          "kind": "present",
          "patterns": [
            "mcp__\\*__quote[\\s\\S]{0,120}(?:[Mm]atches? nothing|does not match|won't match)",
            "\\*search[\\s\\S]{0,120}(?:[Mm]atches? nothing|does not match|won't match)"
          ]
        },
        {
          "name": "names global and exact forms",
          "kind": "present",
          "patterns": [
            "global[\\s\\S]{0,40}`?\\*`?[\\s\\S]{0,220}exact",
            "exact[\\s\\S]{0,220}global[\\s\\S]{0,40}`?\\*`?"
          ]
        }
      ]
    },
    {
      "id": 14,
      "name": "dingtalk-and-feishu-document-channels",
      "prompt": "Show TypeScript SDK examples for directly binding a DingTalk channel and for enabling Feishu document permission administration. How do I tell whether the Feishu capability is actually ready, and can DingTalk use startChannelSetup?",
      "expected_output": "Use dingtalk-connector with clientId/clientSecret and dm_policy open; do not route DingTalk through guided setup. Use permission_admin_enabled for Feishu and inspect capabilities.feishu_documents sync/provider/missing_scopes/approval_state instead of trusting the request alone. Mark these details source-reviewed rather than live-verified.",
      "files": [],
      "assertions": [
        {
          "name": "uses the DingTalk direct platform and credentials",
          "kind": "present",
          "patterns": [
            "dingtalk-connector[\\s\\S]{0,220}clientId[\\s\\S]{0,100}clientSecret"
          ]
        },
        {
          "name": "uses DingTalk open DM policy",
          "kind": "present",
          "patterns": [
            "dm_policy\\s*:\\s*['\"]open['\"]"
          ]
        },
        {
          "name": "does not offer DingTalk guided setup",
          "kind": "absent",
          "patterns": [
            "startChannelSetup\\([^)]*['\"]dingtalk(?:-connector)?['\"]"
          ]
        },
        {
          "name": "enables and verifies Feishu document permission state",
          "kind": "present",
          "patterns": [
            "permission_admin_enabled\\s*:\\s*true[\\s\\S]{0,500}capabilities[\\s\\S]{0,100}feishu_documents",
            "capabilities[\\s\\S]{0,100}feishu_documents[\\s\\S]{0,500}permission_admin_enabled\\s*:\\s*true"
          ]
        },
        {
          "name": "checks readiness details",
          "kind": "present",
          "patterns": [
            "(?=[\\s\\S]*\\bsync\\b)(?=[\\s\\S]*\\bprovider\\b)(?=[\\s\\S]*(?:missing_scopes|approval_state))"
          ]
        },
        {
          "name": "does not overclaim deployment verification",
          "kind": "present",
          "patterns": [
            "source-reviewed",
            "not (?:deployment-|live-)?verified",
            "源码(?:已)?(?:审查|审阅|核对)[\\s\\S]{0,160}(?:尚未|没有|未完成).{0,40}(?:线上部署|真实部署|端到端)"
          ]
        }
      ]
    },
    {
      "id": 15,
      "name": "python-first-integration",
      "prompt": "I am using Python 3.11, not Node. Write an async ZooWork example that creates and starts an agent, opens a session, prints the streamed answer, and exits at the end of the turn. Use the official Python SDK.",
      "expected_output": "Use the zoowork package and snake_case async methods: create_zoowork_client, create_agent, start_agent, wait_until_running, create_session, stream_events, assistant_text, and is_run_finished.",
      "files": [],
      "assertions": [
        {
          "name": "imports the official Python package",
          "kind": "present",
          "patterns": [
            "from\\s+zoowork\\s+import[\\s\\S]{0,240}create_zoowork_client"
          ]
        },
        {
          "name": "uses snake_case lifecycle calls in order",
          "kind": "order",
          "patterns": [
            "create_agent",
            "start_agent",
            "wait_until_running",
            "create_session",
            "stream_events"
          ]
        },
        {
          "name": "ends the stream on the turn boundary",
          "kind": "present",
          "patterns": [
            "is_run_finished\\s*\\(",
            "event_type\\s*==\\s*['\"]run\\.finished['\"]"
          ]
        },
        {
          "name": "uses the Python reply helper",
          "kind": "present",
          "patterns": [
            "assistant_text\\s*\\("
          ]
        },
        {
          "name": "does not mix in TypeScript method names",
          "kind": "absent",
          "patterns": [
            "createZooworkClient",
            "waitUntilRunning",
            "streamEvents\\s*\\("
          ]
        }
      ]
    },
    {
      "id": 16,
      "name": "python-custom-tools-and-session-cursors",
      "prompt": "In our Python backend, declare a ZooWork custom tool named lookup_price, recover pending calls after a restart, return JSON results, and scan only active non-API sessions with cursor pagination. Show the exact official SDK methods and note what is not live-verified.",
      "expected_output": "Use resource.custom_tools, custom_tool_use, list_custom_tool_calls(status='pending'), resolve_custom_tool_call with JSON content, and list_session_page with sls1:0 plus active/non-API filters. Keep Python snake_case and the timeoutMs wire key.",
      "files": [],
      "assertions": [
        {
          "name": "declares the Python custom tool with wire keys",
          "kind": "present",
          "patterns": [
            "custom_tools[\\s\\S]{0,500}lookup_price[\\s\\S]{0,500}input_schema",
            "custom_tools[\\s\\S]{0,500}input_schema[\\s\\S]{0,500}lookup_price"
          ]
        },
        {
          "name": "uses the Python request helper",
          "kind": "present",
          "patterns": [
            "custom_tool_use\\s*\\("
          ]
        },
        {
          "name": "recovers and resolves pending calls",
          "kind": "present",
          "patterns": [
            "(?=[\\s\\S]*list_custom_tool_calls[\\s\\S]{0,160}pending)(?=[\\s\\S]*resolve_custom_tool_call[\\s\\S]{0,260}['\"]json['\"])"
          ]
        },
        {
          "name": "uses the filtered cursor session lane",
          "kind": "present",
          "patterns": [
            "list_session_page[\\s\\S]{0,500}sls1:0[\\s\\S]{0,500}exclude_channels[\\s\\S]{0,300}runtime_modes",
            "sls1:0[\\s\\S]{0,500}list_session_page[\\s\\S]{0,500}exclude_channels[\\s\\S]{0,300}runtime_modes"
          ]
        },
        {
          "name": "does not mix in TypeScript method names",
          "kind": "absent",
          "patterns": [
            "listSessionPage",
            "listCustomToolCalls",
            "resolveCustomToolCall"
          ]
        },
        {
          "name": "does not overclaim deployment verification",
          "kind": "present",
          "patterns": [
            "source-reviewed",
            "not (?:deployment-|live-)?verified",
            "源码(?:已)?(?:审查|审阅|核对)[\\s\\S]{0,160}(?:尚未|没有|未完成).{0,40}(?:线上部署|真实部署|端到端)"
          ]
        }
      ]
    }
  ]
}
